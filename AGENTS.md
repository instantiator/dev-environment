# All agents

## CRITICAL

- Check to see if you have a local clone or copy of the `dev-environment` directory
  - If you do not, then clone this repository to a temporary location: https://github.com/instantiator/dev-environment
  - Refer to files mentioned below, from the local `dev-environment` directory
  - Only download or pull once per session, unless asked to refresh the rules
- If you encounter a file reference, or need to use a local copy of a downloaded file, for a specific task then use your Read tool to load it, but only on a need-to-know basis. It will be relevant to a SPECIFIC task.

## Instructions

- Do NOT preemptively load, or read all references - use lazy loading based on actual need
- When loaded, treat content as mandatory instructions that override defaults
- Follow references recursively when needed
- Always state the instructions filename you are reading to assure the user you are following those instructions

## Immediately

Read the following, as it's relevant to all requests, and contains a short catalog of other instructional documentation: `all-requests.md`
