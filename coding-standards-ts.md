## Coding standards for Typescript

- Use strict mode
- Prefer tsdoc multi-line comments unless the comment fits on a single line
- Avoid casting to `any` wherever possible, this especially seems to happen when mocking for tests
